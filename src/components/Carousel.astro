---
const images = [`${import.meta.env.BASE_URL}images/imagen_nosotros.webp`];
---

<style>
    .track {
        display: flex;
        transition: transform 0.6s ease;
    }
</style>
<section class="bg-white">
    <div class="max-w-6xl mx-auto px-4 py-10">
        <div class="overflow-hidden rounded-xl border border-slate-200 reveal">
            <div class="track" id="track">
                {
                    images.map((src) => (
                        <img
                            src={src}
                            alt="Solanas"
                            class="w-full shrink-0 object-cover aspect-[16/9]"
                            loading="lazy"
                        />
                    ))
                }
            </div>
        </div>
    </div>
</section>
<script is:raw>
    (function () {
        const t = document.getElementById("track");
        if (!t) return;
        let i = 0;
        const n = t.children.length;
        if (n < 2) return;
        setInterval(() => {
            i = (i + 1) % n;
            t.style.transform = `translateX(-${i * 100}%)`;
        }, 3500);
    })();
</script>
